name: Build Android APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flet
          # 依存関係がある場合はインストール
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.0'
          channel: 'stable'

      - name: Accept Android Licenses
        run: yes | flutter doctor --android-licenses

      - name: Build APK
        run: |
          # 1. アプリのソースコードがあるフォルダへ確実に移動
          if [ -d "src/gymkhana_mobile" ]; then
            cd src/gymkhana_mobile
            echo "📂 Working directory: $(pwd)"
          else
            echo "❌ src/gymkhana_mobile not found!"
            exit 1
          fi

          # 2. main.py が存在することを確認 (Fletビルドのデフォルト)
          if [ ! -f "main.py" ]; then
            if [ -f "app.py" ]; then
              mv app.py main.py
              echo "🔄 Renamed app.py to main.py"
            else
              echo "❌ main.py or app.py not found in $(pwd)"
              ls -l
              exit 1
            fi
          fi

          # 3. 構成ファイルのリストを表示 (デバッグ用)
          echo "📦 Current files for packaging:"
          ls -F

          # 4. ビルド実行
          # エラー解消: Flet CLI 0.80.2 では --project-dir 引数は不要（または未対応）です。
          # cd でディレクトリ移動済みのため、引数なしで実行してカレントディレクトリをビルドします。
          flet build apk --verbose

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release.apk
          # すべてのサブディレクトリからapkを探してアップロード
          path: "**/build/apk/*.apk"

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          # リリースには完成したAPKのみを添付
          files: "**/build/apk/app-release.apk"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
